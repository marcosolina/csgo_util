server:
  port: 8088

com:
  marco:
   ixigo:
     serverhelper:
       demFilesFolderFullPath: "${ENV_CSGO_INSTALL_FOLDER}/csgo"
       services:
         demmanager:
           protocol: http                                                                                         # HTTP or HTTPs
           host: 192.168.1.30                                                                                     # Host name of the Ixigo Proxy service
         eventdispatcher:
           protocol: http                                                                                         # HTTP or HTTPs
           host: 192.168.1.30
           sendEvents: true