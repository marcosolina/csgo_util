logging:
  file:
    name: ./logs/ixigo-rcon-api.log
  level:
    com:
      ixigo:
        rconapi: DEBUG

server:
  servlet:
    context-path: /rcon
    
com:
  ixigo:
    rconapi:
      version: ^project.version^
